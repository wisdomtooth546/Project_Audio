Code to run a music classification model based on the genre.This is done in two different ways:

a) The spectral_feature_analysis.py uses the spectral features extracted from the input data for analysis.
b) The second model is trained using the spectrogram of each music using convolutional neural networks.
